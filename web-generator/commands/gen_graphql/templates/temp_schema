let type = `
type {{modelName}} {
  {{#fieldsArray}}
  {{name}}: {{graphType}}
  {{/fieldsArray}}
}

input {{modelName}}Input {
  {{#fieldsArray}}
  {{name}}: {{graphType}}
  {{/fieldsArray}}
}
`;

let query = `
  {{modelNameSnakeCase}}_get_all: [{{modelName}}]
`;

let mutation = `
  {{modelNameSnakeCase}}_add(data: {{modelName}}Input): {{modelName}}
`;

module.exports = {
  type,
  query,
  mutation,
}