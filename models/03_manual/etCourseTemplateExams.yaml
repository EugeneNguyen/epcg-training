defaultOrder:
  field: name
  order: ASC
formAdd: {}
tableList:
  searchBy: name
fields:
  id:
    list:
      enable: false
  name:
    list:
      link: /etCourseTemplateExam/${item.id}
  createdAt:
    list:
      enable: false
    add:
      enable: false
    edit:
      enable: false
  updatedAt:
    list:
      enable: false
    add:
      enable: false
    edit:
      enable: false
