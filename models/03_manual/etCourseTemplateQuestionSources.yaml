fields:
  id:
    list:
      enable: false
  name:
    list:
      link: /etCourseTemplateQuestionSource/${item.id}
  createdAt:
    list:
      enable: false
    add:
      enable: false
    edit:
      enable: false
  updatedAt:
    list:
      enable: false
    add:
      enable: false
    edit:
      enable: false
