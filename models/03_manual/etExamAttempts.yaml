fields:
  id:
    list:
      link: /etExamAttempt/${item.id}
  userId:
    list:
      enable: false
    add:
      enable: false
    edit:
      enable: false
  duration:
    list:
      link: http://localhost:4000/#/exam/attempts/${item.id}
  createdAt:
    list:
      enable: false
  updatedAt:
    list:
      enable: false
